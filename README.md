# Nexus — Connection Canvas

A polished, interactive spatial workspace for turning scattered thoughts into a living system. Nexus visualizes relationships with animated connectors, signal flow, collaborative presence, and an adaptive details panel.

## Highlights

- Drag-and-drop nodes with live connector geometry
- Animated directional signal flow across the graph
- Create nodes and draw new connections interactively
- Search and command palette (`⌘/Ctrl + K`)
- Zoom, focus, grid, story, share, and inspector controls
- Responsive desktop, tablet, and mobile layouts
- No backend or credentials required

## Run locally

```bash
npm install
npm run dev
```

Create a production build with:

```bash
npm run build
```
